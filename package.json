{
  "name": "portfolio-v4",
  "version": "0.0.1",
  "author": "Nicholas Cannon",
  "private": true,
  "description": "Statically generated portfolio website built with React, Next.js, TypeScript and AWS.",
  "scripts": {
    "bootstrap": "concurrently \"npm install --prefix client\" \"npm install --prefix infra\"",
    "start": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "lint": "npm run lint --prefix client",
    "deploy": "npm run build && cd infra && pulumi up"
  },
  "devDependencies": {
    "concurrently": "^6.2.1"
  }
}
